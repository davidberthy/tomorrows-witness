import React from 'react';
import { createRoot } from 'react-dom/client';
import TomorrowsWitness from './App.jsx';

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TomorrowsWitness />
  </React.StrictMode>
);
